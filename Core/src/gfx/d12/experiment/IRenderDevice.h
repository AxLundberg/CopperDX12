#pragma once

namespace CPR::GFX
{
	class IRenderDevice
	{
	public:
		IRenderDevice();
		~IRenderDevice() = default;

	private:

	};

	IRenderDevice::IRenderDevice()
	{
	}

	IRenderDevice::~IRenderDevice()
	{
	}
}